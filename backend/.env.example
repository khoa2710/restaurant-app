# Copy this file to `.env` in this same folder (backend/).
# Never commit `.env` — it is listed in the repo root `.gitignore`.

PORT=3000

DB_USER=your_db_user
DB_HOST=localhost
DB_NAME=your_db_name
DB_PASSWORD=your_db_password
DB_PORT=5432
