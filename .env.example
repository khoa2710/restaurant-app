# Reference defaults for the backend. The actual file used at runtime is
# `backend/.env` — see `backend/.env.example` and copy that one when running.

PORT=3000

DB_USER=your_db_user
DB_HOST=localhost
DB_NAME=restaurant_app
DB_PASSWORD=your_db_password_here
DB_PORT=5432
